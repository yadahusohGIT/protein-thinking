# Data

- `raw/food_catalog.csv` contains the example prices, nutrition values and serving limits used by the model.
- `processed/` contains the meal plans, summaries and SQLite database produced by `python -m src.run_analysis`.

The prices are illustrative and are not a live supermarket feed.
